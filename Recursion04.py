#BackJoon Algorithm NO.24060
#Recursion

def merge(arr1, arr2):
    sorted_a = []
    t = 0
    while (arr1[])
    
def merge_sort(arr):
    if len(arr) <= 1:
        return arr
    mid = len(arr) // 2
    arr1 = merge_sort(arr[:mid])
    arr2 = merge_sort(arr[mid:])
    merge(arr1, arr2)
    

    