#BackJoon Algorithm NO.24060
#Recursion

def merge(arr1):
    sorted_a = []
    while ()
    
def merge_sort(arr):
    if len(arr) <= 1:
        return arr
    mid = len(arr) // 2
    arr1 = merge_sort(arr[:mid])
    arr2 = merge_sort(arr[mid:])
    merge(arr1, arr2)
    

    